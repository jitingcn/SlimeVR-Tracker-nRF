# XIAO nRF54L15: the nRF54L15 has no native USB controller and the USB-C
# connector is served by the onboard SAMD11 USB-UART bridge on uart20.
# Run MCUboot serial recovery over uart20 instead of USB CDC ACM.
CONFIG_BOOT_SERIAL_UART=y
CONFIG_BOOT_SERIAL_CDC_ACM=n

