# XIAO nRF54LM20A: the nRF54 native USB (usbhs) is not wired to the USB-C
# connector; the onboard SAMD11 serves it as a USB-UART bridge on uart20.
# Run MCUboot serial recovery over uart20 instead of USB CDC ACM.
CONFIG_BOOT_SERIAL_UART=y
CONFIG_BOOT_SERIAL_CDC_ACM=n
